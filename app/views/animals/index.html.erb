<h2 class="center">All Pets</h2>
<div class="row">
  <div class="pad-left-20 pad-right-20">
    <% @animals.each do |animals| %>
        <%= render partial: "shared/all_pets", locals: { element: animals } %>
    <% end %>
  </div>
</div>
